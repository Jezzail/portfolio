class Icon {
  link: string;
  icon: string;
  label: string;

  constructor(link: string, icon: string, label: string) {
    this.link = link;
    this.icon = icon;
    this.label = label;
  }
}

export default Icon;
